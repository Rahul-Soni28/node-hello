pipeline{
    agent any
    stages{
        stage("Build"){
            steps{
                sh 'npm pack'
                sh 'set BUILD_ID=dontKillMe nohup npm run start &'
            }
        }
    }
}